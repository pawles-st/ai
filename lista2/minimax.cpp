#include <algorithm>
#include <iostream>
#include <utility>
#include <string>
#include <vector>

const int BOARD_WIDTH = 5;
const int BOARD_HEIGHT = 5;
const int BOARD_AREA = BOARD_WIDTH * BOARD_HEIGHT;
using Tile = std::pair<int, int>;
using Board = std::unordered_map<Tile, short>;

Board b;

std::vector<>

int main() {
	std::fill(b, std::pair<int, int>(0, 0));	
}
